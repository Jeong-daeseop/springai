# Spring AI MCP Server — 아키텍처 심층 분석

Spring Boot 4.0.6 + Spring AI 2.0.0-M8 기반 eGovFrame 5.x CRUD 소스 자동 생성 AI 플랫폼

분석일: 2026-06-07

---

## 1. 전체 시스템 아키텍처

```mermaid
flowchart TB
    subgraph Clients["Client Layer"]
        CD["Claude Desktop<br/>(MCP SSE Client)"]
        Browser["Web Browser<br/>(Thymeleaf chat.html)"]
        ExtAPI["External API Client<br/>(X-API-Key Auth)"]
    end

    subgraph Server["Spring Boot 4.0.6 Server :8080"]
        subgraph Transport["Transport Layer"]
            SSE_EP["/sse + /mcp/**<br/>MCP SSE Endpoint"]
            WEB_EP["/ai/rag/stream<br/>/ai/simple/stream<br/>SSE Streaming"]
            REST_EP["/api/tools/**<br/>/api/rag/**<br/>REST JSON"]
            DOC_EP["/api/documents/**<br/>Document Mgmt"]
            STATIC["/ (Thymeleaf)<br/>/api/chat/**<br/>/api/ollama/**"]
        end

        subgraph MCP["MCP Tool Layer (19 Tools)"]
            CRUD_T["CrudPromptBuilderTool"]
            SCHEMA_T["SchemaReaderTool"]
            RAG_T["RagTool"]
            CODE_T["CodeTemplateTool<br/>CodeSaverTool<br/>CodeValidatorTool"]
            SEC_T["SecurityTemplateTool"]
            PROJ_T["ProjectInitializrTool<br/>ProjectScannerTool<br/>ProjectHealthTool"]
            OTHER_T["EmployeeTool / DateTimeTool<br/>CommonCodeTool / MenuTool<br/>AuthTool / SqlTool<br/>WorkflowGuideTool<br/>GenerationHistoryTool<br/>OutputPathResolverTool"]
        end

        subgraph Chat["Web Chat Subsystem"]
            CHAT_CTRL["EgovOllamaChatController"]
            CHAT_SVC["EgovSessionAwareChatServiceImpl"]
            DOC_SVC["EgovDocumentServiceImpl"]
            SESSION_SVC["EgovChatSessionServiceImpl"]
        end

        subgraph Core["Core Services"]
            LLM_ROUTER["LlmRouterService"]
            CTX_ASM["ContextAssembler"]
            PROMPT["EgovPromptBuilder"]
            CRUD_SVC["CrudPromptBuilderService"]
            CODE_SVC["CodeService"]
            RAG_SVC["RagService"]
            COMPRESS["EgovCompressionQueryTransformer"]
        end
    end

    subgraph External["External Systems"]
        OPENAI["OpenAI API<br/>gpt-4o-mini / gpt-4o"]
        OLLAMA["Ollama (Local)<br/>qwen3:8b / qwen3:1.7b"]
        MYSQL["MySQL 8.0<br/>(Docker: egov-mysql)<br/>DB: com"]
        REDIS["Redis Stack :6379<br/>VectorStore + ChatMemory"]
        ONNX["ko-sroberta ONNX<br/>(Local Model)"]
        FS["File System<br/>~/Desktop/egov-generated"]
    end

    CD -->|"JSON-RPC over SSE"| SSE_EP
    Browser -->|"HTTP SSE Stream"| WEB_EP
    Browser -->|"HTTP"| STATIC
    ExtAPI -->|"HTTP + X-API-Key"| REST_EP
    Browser -->|"HTTP"| DOC_EP

    SSE_EP --> MCP
    WEB_EP --> CHAT_CTRL
    REST_EP --> Core
    DOC_EP --> DOC_SVC

    CHAT_CTRL --> CHAT_SVC
    CHAT_SVC --> LLM_ROUTER
    CHAT_SVC --> CTX_ASM
    CHAT_SVC --> COMPRESS

    MCP --> Core
    CRUD_T --> CRUD_SVC
    CRUD_T --> CODE_SVC

    LLM_ROUTER -->|"gpt-*/o1/o3"| OPENAI
    LLM_ROUTER -->|"default/SENSITIVE"| OLLAMA
    COMPRESS -->|"qwen3:1.7b"| OLLAMA
    RAG_SVC --> REDIS
    CRUD_SVC --> MYSQL
    CODE_SVC --> FS
    ONNX --> REDIS
    CHAT_SVC -.->|"ChatMemory"| REDIS
```

---

## 2. 레이어 구조

```mermaid
flowchart TB
    subgraph ToolLayer["Tool Layer (MCP @Tool — 19개)"]
        direction LR
        T1["CrudPromptBuilderTool"] 
        T2["SchemaReaderTool"]
        T3["RagTool"]
        T4["CodeSaverTool / CodeTemplateTool / CodeValidatorTool"]
        T5["SecurityTemplateTool"]
        T6["ProjectInitializrTool / ProjectScannerTool / ProjectHealthTool"]
        T7["EmployeeTool / CommonCodeTool / MenuTool / AuthTool"]
        T8["SqlTool / WorkflowGuideTool / GenerationHistoryTool / DateTimeTool / OutputPathResolverTool"]
    end

    subgraph ControllerLayer["Controller Layer (REST/SSE — 7개)"]
        direction LR
        C1["EgovOllamaChatController<br/>/ai/rag/stream, /ai/simple/stream"]
        C2["ToolApiController<br/>/api/tools/**"]
        C3["RagController<br/>/api/rag/**"]
        C4["EgovDocumentController<br/>/api/documents/**"]
        C5["EgovChatSessionController / EgovOllamaModelController"]
        C6["EgovWebController<br/>/ (Thymeleaf)"]
    end

    subgraph ServiceLayer["Service Layer (23개)"]
        direction LR
        S1["LlmRouterService"]
        S2["CrudPromptBuilderService"]
        S3["CodeService / CodeValidatorService"]
        S4["RagService / ChunkService"]
        S5["SchemaService / TableRelationService"]
        S6["ContextAssembler / EgovPromptBuilder"]
        S7["SecurityTemplateService / ProjectInitializrService"]
        S8["EmployeeService / CommonCodeService / MenuService"]
        S9["GenerationHistoryService / SqlService / AuthService"]
        S10["EgovSessionAwareChatServiceImpl / EgovDocumentServiceImpl / EgovChatSessionServiceImpl"]
    end

    subgraph RepoLayer["Repository / Data Access"]
        direction LR
        R1["EmployeeRepository (JdbcTemplate)"]
        R2["GenerationHistoryRepository (JdbcTemplate)"]
        R3["RedisVectorStore"]
        R4["EgovRedisChatMemoryRepository"]
    end

    subgraph DataLayer["Data Stores"]
        direction LR
        D1[("MySQL 8.0<br/>egov-mysql")]
        D2[("Redis Stack<br/>VectorStore + Memory")]
        D3[("File System<br/>egov-generated")]
    end

    ToolLayer --> ServiceLayer
    ControllerLayer --> ServiceLayer
    ServiceLayer --> RepoLayer
    RepoLayer --> DataLayer
```

---

## 3. LLM 라우팅 결정 트리

```mermaid
flowchart TD
    START(["LlmRouterService.route(modelOrTaskType)"])

    NULL_CHECK{"modelOrTaskType<br/>== null?"}
    PREFIX_CHECK{"gpt- / o1 / o3<br/>접두사?"}
    TASK_SWITCH{"taskType<br/>toUpperCase()"}

    OPENAI_CLIENT["OpenAI ChatClient<br/>(gpt-4o-mini)"]
    OLLAMA_CLIENT["Ollama ChatClient<br/>(qwen3:8b)"]
    ERROR["IllegalArgumentException<br/>'Claude Desktop MCP SSE<br/>채널로 처리합니다'"]

    START --> NULL_CHECK
    NULL_CHECK -->|"yes"| OLLAMA_CLIENT
    NULL_CHECK -->|"no"| PREFIX_CHECK
    PREFIX_CHECK -->|"yes (모델명 기반)"| OPENAI_CLIENT
    PREFIX_CHECK -->|"no"| TASK_SWITCH

    TASK_SWITCH -->|"CLASSIFICATION<br/>SIMPLE_QUERY"| OPENAI_CLIENT
    TASK_SWITCH -->|"SENSITIVE_DATA<br/>default"| OLLAMA_CLIENT
    TASK_SWITCH -->|"CODE_GENERATION"| ERROR

    subgraph WebChat["웹 채팅 경로 (EgovSessionAwareChatServiceImpl)"]
        SELECT["selectClient(model)<br/>llmRouterService.route(model) 위임"]
        SELECT -->|"gpt-4o-mini 등"| OPENAI_CLIENT
        SELECT -->|"qwen3:8b 등"| OLLAMA_CLIENT
    end

    subgraph MCPPath["MCP 경로 (Claude Desktop)"]
        CLAUDE["Claude (Direct)<br/>MCP SSE Transport<br/>JSON-RPC over /sse"]
    end

    style OPENAI_CLIENT fill:#4CAF50,color:#fff
    style OLLAMA_CLIENT fill:#2196F3,color:#fff
    style CLAUDE fill:#9C27B0,color:#fff
    style ERROR fill:#F44336,color:#fff
```

---

## 4. RAG 파이프라인

```mermaid
sequenceDiagram
    participant User as User/Client
    participant DocCtrl as EgovDocumentController
    participant DocSvc as EgovDocumentServiceImpl
    participant RagSvc as RagService
    participant ChunkSvc as ChunkService
    participant ONNX as ko-sroberta ONNX
    participant Redis as Redis VectorStore
    participant ChatCtrl as EgovOllamaChatController
    participant ChatSvc as EgovSessionAwareChatServiceImpl
    participant Compress as EgovCompressionQueryTransformer
    participant CtxAsm as ContextAssembler
    participant QAAdvisor as QuestionAnswerAdvisor
    participant LLM as OpenAI / Ollama

    Note over User,Redis: Ingestion (임베딩) 경로

    User->>DocCtrl: POST /api/documents/reindex
    DocCtrl->>DocSvc: reindexDocuments() [비동기]
    DocSvc->>DocSvc: Walk documentPaths (.md/.pdf)
    DocSvc->>DocSvc: SHA Hash 비교 → 변경 파일만 처리
    DocSvc->>RagSvc: ingestText(docId, content, "document")
    RagSvc->>RagSvc: deleteOldChunks(docId)
    RagSvc->>ChunkSvc: chunk(content) 700자/100자 overlap
    ChunkSvc-->>RagSvc: List chunks
    RagSvc->>ONNX: embed(chunks)
    ONNX-->>RagSvc: 768차원 벡터
    RagSvc->>Redis: vectorStore.add(docs)
    RagSvc->>Redis: chunk ID 저장 (재임베딩 삭제용)

    Note over User,LLM: Retrieval (검색) 경로

    User->>ChatCtrl: GET /ai/rag/stream?message=...&sessionId=...
    ChatCtrl->>ChatSvc: streamRagResponse(query, model, sessionId)
    ChatSvc->>Compress: compress(query, sessionId)
    Compress->>LLM: Ollama qwen3:1.7b 쿼리 압축
    Compress-->>ChatSvc: searchQuery
    ChatSvc->>CtxAsm: build(searchQuery)
    CtxAsm->>CtxAsm: MySQL: Schema + 생성이력 + 테이블관계 (6,000자 예산)
    CtxAsm-->>ChatSvc: context
    ChatSvc->>QAAdvisor: VectorStore 검색 (topK=10, threshold=0.30, type==document)
    QAAdvisor->>Redis: similaritySearch(query)
    Redis-->>QAAdvisor: 유사 문서
    ChatSvc->>LLM: stream() + ChatMemory Advisor
    LLM-->>User: SSE 스트리밍
```

---

## 5. MCP Tool 호출 흐름 (CRUD 소스 생성)

```mermaid
sequenceDiagram
    participant Claude as Claude Desktop
    participant MCP as MCP SSE Transport
    participant CRUD as CrudPromptBuilderTool
    participant CrudSvc as CrudPromptBuilderService
    participant CodeSvc as CodeService
    participant Validator as CodeValidatorService
    participant DB as MySQL
    participant FS as File System

    Note over Claude,FS: auto 모드 (서버 자체 생성 — 토큰 절감)

    Claude->>MCP: buildFullCrudPrompt(..., "auto")
    MCP->>CRUD: @Tool 메서드 호출
    CRUD->>CrudSvc: buildPlaceholderValues(db, table)
    CrudSvc->>DB: INFORMATION_SCHEMA.COLUMNS 조회
    DB-->>CrudSvc: 컬럼/PK/타입 메타데이터
    CrudSvc-->>CRUD: PlaceholderValues

    loop 11개 레이어 (vo, mapper, mapperXml, service, serviceImpl, controller, controllerAdvice, jspList, jspDetail, jspRegist, jspUpdt)
        CRUD->>CodeSvc: generateSource(layer, values)
        CodeSvc->>CodeSvc: 템플릿 치환 (placeholder → 실제값)
        CodeSvc-->>CRUD: 생성 소스
        CRUD->>CodeSvc: saveGeneratedCode(filePath, code)
        CodeSvc->>FS: Files.writeString()
    end

    CRUD->>Validator: validateDirectory(outputPath)
    CRUD-->>Claude: 생성 결과 요약

    Note over Claude,FS: claude 모드 (LLM 주도 생성)

    Claude->>MCP: buildFullCrudPrompt(..., "claude")
    MCP->>CRUD: @Tool 메서드 호출
    CRUD->>CrudSvc: buildFullCrudPrompt()
    CrudSvc->>DB: 컬럼 정보 조회
    CrudSvc-->>CRUD: 프롬프트 문자열 반환
    CRUD-->>Claude: 프롬프트 텍스트

    loop Claude가 레이어별 Tool 순차 호출
        Claude->>MCP: getCodeTemplate(layer)
        Claude->>MCP: generateSource(layer, valuesJson)
        Claude->>MCP: saveGeneratedCode(filePath, code)
    end
    Claude->>MCP: validateGeneratedCodeDirectory()
    Claude->>MCP: saveGenerationHistory()
```

---

## 6. 웹 채팅 RAG 응답 시퀀스

```mermaid
sequenceDiagram
    actor User as Browser (chat.html)
    participant Ctrl as EgovOllamaChatController
    participant ChatSvc as EgovSessionAwareChatServiceImpl
    participant Router as LlmRouterService
    participant Compress as EgovCompressionQueryTransformer
    participant CtxAsm as ContextAssembler
    participant Prompt as EgovPromptBuilder
    participant QA as QuestionAnswerAdvisor
    participant Memory as MessageChatMemoryAdvisor
    participant Redis as Redis
    participant LLM as OpenAI/Ollama

    User->>Ctrl: GET /ai/rag/stream<br/>?message=COMTNEMPLYRINFO CRUD<br/>&model=gpt-4o-mini&sessionId=abc123

    Ctrl->>Ctrl: setupSession(abc123)
    Ctrl->>ChatSvc: streamRagResponse(query, model, sessionId)

    ChatSvc->>Compress: compress(query, abc123)
    Compress->>Redis: ChatMemory 히스토리 로드
    Compress->>LLM: qwen3:1.7b 쿼리 압축
    LLM-->>Compress: 독립 검색 쿼리
    Compress-->>ChatSvc: searchQuery

    ChatSvc->>CtxAsm: build(searchQuery)
    CtxAsm->>CtxAsm: extractTableName → COMTNEMPLYRINFO
    CtxAsm->>CtxAsm: [1순위] DB 스키마
    CtxAsm->>CtxAsm: [2순위] 생성 이력
    CtxAsm->>CtxAsm: [3순위] 테이블 관계
    CtxAsm-->>ChatSvc: context (max 6,000자)

    ChatSvc->>Router: route("gpt-4o-mini")
    Router-->>ChatSvc: OpenAI ChatClient

    ChatSvc->>Prompt: assembledSystemPrompt(context)
    Prompt-->>ChatSvc: system prompt

    Note over ChatSvc,Redis: QuestionAnswerAdvisor: VectorStore 검색<br/>type==document, topK=10, threshold=0.30

    ChatSvc->>LLM: gpt-4o-mini stream + advisors
    LLM-->>ChatSvc: Flux<ChatResponse>
    ChatSvc->>Redis: ChatMemory 저장 (세션 abc123)
    ChatSvc-->>Ctrl: Flux<ChatResponse>
    Ctrl-->>User: text/event-stream (SSE)
```

---

## 7. 컴포넌트 의존성 맵

```mermaid
graph TD
    subgraph Config["Configuration Layer"]
        McpConfig --> ToolCallbackProvider["ToolCallbackProvider<br/>(19 Tools)"]
        EgovRagConfig --> OAClient["openAiChatClient"]
        EgovRagConfig --> OLClient["ollamaChatClient"]
        EgovRagConfig --> QAAdv["QuestionAnswerAdvisor"]
        EgovRagConfig --> |"@Primary"| OllamaChatModel
        VectorStoreConfig --> RedisVectorStore
        VectorStoreConfig --> ONNX["TransformersEmbeddingModel<br/>(ko-sroberta)"]
        EgovChatMemoryConfig --> MemAdv["MessageChatMemoryAdvisor"]
    end

    subgraph ChatServices["Chat Subsystem"]
        EgovSessionAwareChatServiceImpl --> LlmRouterService
        EgovSessionAwareChatServiceImpl --> ContextAssembler
        EgovSessionAwareChatServiceImpl --> EgovPromptBuilder
        EgovSessionAwareChatServiceImpl --> QAAdv
        EgovSessionAwareChatServiceImpl --> MemAdv
        EgovSessionAwareChatServiceImpl --> EgovCompressionQueryTransformer
        EgovDocumentServiceImpl --> RagService
        EgovCompressionQueryTransformer --> OllamaChatModel
    end

    subgraph RouterServices["Routing"]
        LlmRouterService --> OAClient
        LlmRouterService --> OLClient
    end

    subgraph CoreServices["Core Services"]
        ContextAssembler --> SchemaService
        ContextAssembler --> TableRelationService
        ContextAssembler --> GenerationHistoryService
        CrudPromptBuilderService --> EgovPromptBuilder
        CrudPromptBuilderService --> JdbcTemplate
        RagService --> RedisVectorStore
        RagService --> ChunkService
    end

    subgraph DataAccess["Data Access"]
        SchemaService --> JdbcTemplate
        EmployeeService --> EmployeeRepository
        GenerationHistoryService --> GenerationHistoryRepository
        JdbcTemplate --> MySQL[("MySQL 8.0")]
        RedisVectorStore --> Redis[("Redis Stack")]
        MemAdv --> Redis
    end
```

---

## 8. 배포 구조

```mermaid
flowchart TB
    subgraph Host["Host Machine (macOS)"]
        subgraph JVM["Java 17 JVM Process"]
            APP["springai-0.0.1-SNAPSHOT.jar<br/>Spring Boot 4.0.6<br/>Embedded Tomcat :8080"]
            ONNX_RT["TransformersEmbeddingModel<br/>ko-sroberta ONNX Runtime<br/>(In-Process)"]
        end

        subgraph Docker["Docker Container"]
            MYSQL["egov-mysql<br/>mysql:8.0 :3306<br/>DB: com / User: com"]
        end

        subgraph LocalServices["Local Services"]
            REDIS["Redis Stack :6379<br/>Index: egov-rag<br/>Prefix: egov:"]
            OLLAMA["Ollama :11434<br/>qwen3:8b (채팅)<br/>qwen3:1.7b (쿼리압축)"]
        end

        subgraph FileSystem["File System"]
            MODELS["~/models/ko-sroberta/<br/>model.onnx + tokenizer.json"]
            DOCS["~/eGovFrame-5.0.0/<br/>md/ + docs/"]
            OUTPUT["~/Desktop/egov-generated/<br/>{domain}/"]
            LOGS["/tmp/springai-mcp.log"]
        end

        subgraph ClaudeDesktop["Claude Desktop"]
            CLAUDE_APP["Claude Desktop<br/>claude_desktop_config.json<br/>→ SSE HTTP 연결"]
        end
    end

    subgraph Cloud["Cloud API"]
        OPENAI["OpenAI API<br/>gpt-4o-mini / gpt-4o"]
    end

    CLAUDE_APP <-->|"JSON-RPC over SSE HTTP"| APP
    APP -->|"JDBC :3306"| MYSQL
    APP -->|"Jedis :6379"| REDIS
    APP -->|"HTTP :11434"| OLLAMA
    APP -->|"HTTPS"| OPENAI
    APP -->|"Read"| MODELS
    APP -->|"Read .md/.pdf"| DOCS
    APP -->|"Write .java/.xml/.jsp"| OUTPUT
    APP -->|"Write"| LOGS
    APP --- ONNX_RT

    style JVM fill:#E3F2FD,stroke:#1565C0
    style Docker fill:#FFF3E0,stroke:#E65100
    style Cloud fill:#F3E5F5,stroke:#7B1FA2
```

---

## 9. 설계 핵심 결정 사항

| 항목 | 설계 결정 | 위치 |
|------|----------|------|
| MCP Tool 등록 | 19개 Tool을 단일 `ToolCallbackProvider` 빈으로 통합 | `McpConfig.java:37-64` |
| LLM 라우팅 | 모델명 접두사 또는 taskType 문자열 매칭 | `LlmRouterService.java:35-51` |
| 기본 ChatModel | Ollama를 `@Primary`로 지정 | `EgovRagConfig.java:27-29` |
| CRUD auto 모드 | 서버 사이드 템플릿 치환 — LLM 토큰 0 소비 | `CrudPromptBuilderTool.java:90-175` |
| 컨텍스트 예산 | 6,000자 한도 내 우선순위 조립 | `ContextAssembler.java:38` |
| 청크 전략 | 700자/100자 overlap, 자연경계 분할 | `ChunkService.java:27-31` |
| 문서 재인덱싱 | SHA Hash 비교 → 변경 파일만 처리 | `EgovDocumentServiceImpl.java:114-126` |
| VectorStore 필터 | `type == 'document'` (소스코드/이력 제외) | `EgovRagConfig.java:45-52` |
| 로컬 임베딩 | ko-sroberta ONNX (외부 API 불필요) | `application.yaml:22-35` |
| SSE 유지 | Tomcat connection-timeout 1시간, async 무제한 | `application.yaml:65-70` |

---

## 10. Trade-offs 요약

| 설계 선택 | 장점 | 단점 |
|-----------|------|------|
| 3-LLM 하이브리드 | 비용/품질/보안 최적화 | 운영 복잡도, 장애 포인트 3배 |
| auto 모드 (결정적 생성) | 토큰 97% 절감, 일관된 품질 | 커스터마이징 유연성 감소 |
| 로컬 ONNX 임베딩 | 비용 0, 한국어 특화 | GPU 없을 경우 대량 임베딩 느림 |
| Redis 통합 (VectorStore+Memory+세션) | 인프라 단순화 | 단일 장애점 |
| 이중 인터페이스 (MCP+웹) | Claude/브라우저 모두 지원 | 유지보수 복잡도 증가 |
| 쿼리 압축 (qwen3:1.7b) | 검색 정확도 향상 | Reactive 파이프라인 동기 블로킹 |
