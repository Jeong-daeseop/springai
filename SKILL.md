
---
name: egovframe-common-component
description: 전자정부 표준프레임워크 공통컴포넌트 v5.0 가이드와 패키지 간 참조 관계를 함께 적용할 때 사용. 공통컴포넌트 시작·환경설정·배포·테이블 구성, globals.properties, 새 컴포넌트 추가·의존성·순환 참조 방지·모듈 참조 방향 결정 시 적용.
---

# eGovFrame 공통컴포넌트 가이드 및 패키지 의존성

표준프레임워크 **공통컴포넌트 v5.0** 가이드와 **패키지 간 참조 관계**를 한 스킬에서 적용한다. 실행환경 5.0 기반 가이드를 기준으로 하며, 기능 차이보다 **내부 코드 품질 및 일관된 구성**을 목표로 한다.

**공식 문서**
- [공통컴포넌트 v5.0 init (가이드 진입점)](https://www.egovframe.go.kr/wiki/doku.php?id=egovframework:com:v5.0:init)
- [공통컴포넌트 v5.0 가이드 인덱스(init_guide)](https://www.egovframe.go.kr/wiki/doku.php?id=egovframework:com:v5.0:init_guide) — 컴포넌트별 상세 가이드 링크
- [패키지 간 참조 관계](https://www.egovframe.go.kr/wiki/doku.php?id=egovframework:com:v5.0:init_pkg_dependency)

---

## 1. 가이드 구성 (위키 기준)

### 1.1 표준프레임워크 공통컴포넌트 v5.0 개요

| 항목 | 위키 문서 | 활용 시점 |
|------|-----------|-----------|
| 공통컴포넌트 시작하기 | Getting Started | 프로젝트 셋업, 공통컴포넌트 도입 시 |
| 공통컴포넌트 환경설정 | globals.properties | 경로·DB·파일 업로드 등 전역 설정 시 |
| 공통컴포넌트 배포 파일의 구성 | 배포 파일 구성 | 의존성·모듈 구조·배포물 확인 시 |
| 공통컴포넌트 테이블 구성 정보 | 테이블 구성 | DB 스키마·마이그레이션·테이블 설계 시 |
| 패키지 간 참조 관계 | init_pkg_dependency | 모듈 추가·의존성·순환 참조 검토 시 |

### 1.2 표준프레임워크 공통컴포넌트 v5.0 상세 가이드

- **공통컴포넌트별 가이드(Index)**: 컴포넌트 단위 상세 설명은 위키의 “공통컴포넌트별 가이드” 인덱스에서 해당 컴포넌트 문서로 이동해 참고한다.

### 1.3 공통컴포넌트 v5.0 가이드 인덱스 (init_guide)

실행환경 5.0 기반, v5.0 컴포넌트별 상세 가이드 링크는 [init_guide](https://www.egovframe.go.kr/wiki/doku.php?id=egovframework:com:v5.0:init_guide)에서 제공한다. v5.0 주요 개선: Spring Framework 6.x 및 Jakarta EE 9+(jakarta.* 네임스페이스) 전환, Java 17 LTS 최소 요구사항 적용, Spring Security 6.x 기반 인증·인가 재구성, 보안 취약점 개선 및 KISA 시큐어 코딩 가이드 반영.

**대분류 및 주요 하위 항목**

- **사용자디렉토리/통합인증**: 일반로그인(SSO연계), 인증서로그인, 로그인정책관리, 디지털원패스
- **보안**: 권한관리·권한별 롤관리, 권한그룹관리, 그룹관리, 롤관리, 부서권한관리, 암호화/복호화, 로그인 시간관리, 비밀번호 만료기간 관리
- **통계/리포팅**: 게시물통계, 사용자통계, 접속통계, 화면통계, 보고서통계, 자료이용현황통계
- **협업**
  - 게시판: 블로그관리, 게시판관리, 방명록, 통합게시판, 템플릿관리, 댓글·스크랩·만족도조사
  - 커뮤니티: 커뮤니티 생성/사용/회원관리
  - 문자메시지, 일정관리(부서일정·일지·간부일정·메모할일 등), 전자우편, 주소록/명함록, 전자결재(약식결재)
- **사용자 지원**
  - 사용자관리: 기업회원, 사용자, 회원관리
  - 개인화(마이페이지), 약관관리, 온라인헬프(도움말·용어사전·FAQ·Q&A·온라인매뉴얼)
  - 온라인참여: 상담·설문·회의·POLL관리
  - 정보제공/알림: 뉴스·사이트·배너·팝업·사용자부재·휴가·당직·쪽지·Wiki·RSS·Facebook 연동 등
- **시스템관리**
  - 공통코드관리: 공통분류/상세/코드, 우편번호, 행정/기관/법정동코드, 도로명주소연계
  - 로그관리: 사용로그, 송수신로그, 시스템이력, 웹/접속/개인정보조회 로그
  - 메뉴관리: 메뉴관리, 메뉴생성, 바로가기메뉴
  - 프로그램관리, 배치관리(배치작업/결과, 스케줄), 시스템관리(백업·네트워크·서버정보), 장애관리
- **시스템/서비스연계**: 시스템연계관리, 연계현황/메시지/기관관리
- **디지털 자산관리**: 개인지식관리, 지식맵(조직/유형), 지식전문가·정보·평가관리
- **요소기술**
  - 신규(v3.2): 이중등록 방지, Http Request 정보, 숫자/날짜 지역화, showModalDialog 대체, HTTPS UrlRewrite, 중복 로그인 방지, 출퇴근·Resource close·Basic 로그
  - 외부 추가: EasyBatch, Social Login, LDAP조직도, 웹소켓메신저
  - 메시지처리, 인쇄/출력, 쿠키/세션, 달력(공휴일), 인터페이스/화면(메인·트리메뉴), 웹에디터, 포맷/계산/변환(날짜·문자열·숫자·인코딩 등), 시스템(파일·디렉토리·모니터링·프로퍼티·XML 등)

특정 컴포넌트 구현·설정 시 위 인덱스에서 해당 항목을 찾아 위키 상세 문서를 참고한다.

---

## 2. 패키지 간 참조 관계 (원칙)

1. **순환 참조 금지**: 패키지 간 상호 참조·이행 참조를 통한 순환 참조는 두지 않는다.
2. **참조 종류 구분**:
   - **함수적 참조**: 다른 패키지의 서비스/유틸을 직접 호출하는 관계.
   - **UI·업무적 참조**: 화면/메뉴/업무 흐름상의 연결만 있고, 하위 레이어가 상위 패키지를 참조하지 않도록 한다.
3. **경량화**: 불필요한 상위→하위 참조를 만들지 않고, 공통 레이어는 하위만 참조하도록 유지한다.

---

## 3. 컴포넌트 분류와 참조 방향

아래 분류는 위키의 컴포넌트 분류와 이 템플릿(`egov-template-enterprise`)의 패키지 구조를 맞춘 것이다. **아래에 있는 분류가 위 분류를 참조할 수 있고, 역방향 참조는 피한다.**

| 분류 | 설명 | 이 프로젝트 패키지 대응 |
|------|------|---------------------------|
| **공통(cmm)** | 공통 유틸, 로그인, 파일, 코드, 예외 처리 | `egovframework.com.cmm` |
| **사용자디렉토리/통합인증** | 사용자/디렉토리, 통합인증 | `let.uss.umt`, `let.uat` 등 |
| **보안** | 권한, 역할, 그룹, 접근 제어 | `let.sec` |
| **통계/리포팅** | 접속 통계, 로그 | `let.sts`, `let.sym.log` |
| **협업** | 게시판, 커뮤니티, 블로그, 일정, 문자, 주소록 | `let.cop.bbs`, `let.cop.com` 등 |
| **사용자지원** | 부재관리 등 사용자 지원 기능 | `let.uss.ion.uas` |
| **시스템관리** | 메뉴, 프로그램, 로그, 공통코드, 로그인정책 | `let.sym` (mnu, prm, log, ccm 등), `let.uat` |

- **공통(cmm)**은 모든 업무 모듈에서 참조 가능하다. `cmm`이 업무 전용 패키지(sec, cop, sym 등)를 참조하면 안 된다.
- **시스템관리(sym)**는 메뉴·프로그램·코드 등 기반 데이터를 제공하므로, 다른 업무 모듈이 sym을 참조하는 구조가 자연스럽다. sym이 특정 업무(예: bbs, uss)를 참조하는 것은 피한다.
- **보안(sec)**은 인증·권한 기반이므로 다른 모듈이 sec를 참조한다. sec가 게시판·사용자관리 등 구 업무를 참조하면 순환에 가까워지므로 지양한다.
- **협업(cop)**은 게시판·템플릿 등으로, 필요 시 사용자지원(uss), 시스템관리(sym)를 참조할 수 있다. 반대로 sym/cmm이 cop를 참조하는 것은 지양한다.

---

## 4. 이 프로젝트에서의 대응

- **실행환경**: 이 템플릿은 **실행환경 5.0 / Spring 6** 기반이므로, v5.0 가이드의 “실행환경 5.0” 설명은 버전 차이만 고려해 적용한다.
- **환경설정**: `src/main/resources/egovframework/egovProps/globals.properties` 에서 공통컴포넌트 관련 경로·옵션을 관리한다.
- **테이블/DB**: `DATABASE/` 하위 DB별 DDL·데이터 스크립트가 공통컴포넌트 테이블 구성을 반영한다. 위키의 “테이블 구성 정보”와 대조해 스키마를 맞춘다.
- **패키지 참조**: 위 “2. 패키지 간 참조 관계”, “3. 컴포넌트 분류와 참조 방향”을 따른다.

---

## 5. 작업 시 체크리스트 (통합)

**가이드·환경·DB**
- [ ] 공통컴포넌트 관련 **전역 설정**은 `globals.properties` 또는 `egovProps` 에 두고, 위키의 환경설정 가이드와 맞춘다.
- [ ] **새 테이블/컬럼** 추가 시 위키의 “공통컴포넌트 테이블 구성 정보” 및 `DATABASE/` 스크립트와 충돌이 없는지 확인한다.
- [ ] **배포/모듈 구성** 변경 시 위키의 “배포 파일의 구성”과 실행환경(eGovFrame RTE) 버전을 함께 확인한다.

**패키지·참조**
- [ ] 새로 만드는 클래스가 **상위/공통 패키지**(예: `cmm`)에서 **업무 전용 패키지**(예: `let.cop.bbs`)를 import하지 않는지 확인.
- [ ] A 모듈 → B 모듈 참조 시, B → A 또는 B → C → A 같은 **순환 경로**가 생기지 않는지 확인.
- [ ] 공통 유틸·인터페이스는 `egovframework.com.cmm` 또는 `let.utl`에 두고, 업무 모듈이 이를 참조하도록 유지.
- [ ] Controller → Service → DAO 방향은 유지하고, DAO나 도메인 계층이 Controller/View 전용 패키지를 참조하지 않도록 함.

---

## 6. 참고

- 위키의 “패키지 간 참조 관계” 다이어그램은 분류별로 **함수적 참조**와 **UI·업무적 참조**를 구분해 두었으므로, 실제 구현 시 해당 다이어그램을 함께 확인하는 것이 좋다.
- 프로젝트 전체 패키지 구조와 네이밍 규칙은 프로젝트 루트의 `CLAUDE.md`를 참고한다.
